export interface PathStructure {
  home: string;
  records: string;
  addRecord: string;
  detail: string;
}

export interface TitleStructure {
  home: string;
  records: string;
  addRecord: string;
}
