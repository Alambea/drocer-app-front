import Header from "../Header/Header";
import "./App.scss";

const App = (): React.ReactElement => {
  return (
    <div className="container">
      <Header></Header>
    </div>
  );
};

export default App;
